# Browser Runtime Contract

Browser automation is an execution tool, not an authority source. Capture target, action boundary, observed result, timestamps where freshness matters, and receipts for consequential actions.

Prefer deterministic APIs/connectors over UI automation when equivalent. Never infer durable success from a click alone when the resulting state can be verified directly.
