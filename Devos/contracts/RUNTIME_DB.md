# Runtime Database Contract

A host may materialize DevOS knowledge into a local runtime database for fast deterministic retrieval. The database is a projection/cache, not a new authority source.

Builders must be reproducible from checked-in sources, preserve provenance/authority fields, support integrity checks, and live in generated state excluded from portable distribution defaults.
