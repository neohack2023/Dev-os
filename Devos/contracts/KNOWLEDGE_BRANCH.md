# Knowledge Branch Contract

A knowledge branch is a retrieval and ownership boundary, not necessarily a Git branch. Create one only when a topic has an independently useful context boundary, durable owner/lifecycle, evidence stream, or implementation surface.

Each branch declares a key, scope, status, repository surfaces, dependencies, and optional authority/source pointers. Resolve the smallest branch that can answer the task before loading dependencies.
