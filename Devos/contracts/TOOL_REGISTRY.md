# Tool Registry Contract

Record reusable tools with identity, version/source, capability, supported inputs/outputs, deterministic constraints, safety/authority boundaries, known failure modes, and validation evidence.

A tool registry is operational knowledge. Entries should point to the executable rather than duplicating its implementation. Version changes that may alter behavior require revalidation.
