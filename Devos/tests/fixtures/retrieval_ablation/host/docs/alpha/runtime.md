# Alpha Runtime

The feature-alpha runtime keeps `lantern_mode` deterministic state.

A vector lantern is direct branch evidence. `retry_guard-v2` protects recovery. The runtime mentions beacon once.
